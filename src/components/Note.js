import React from "react";

function Note() {
  return (
    <div>
      <span className="fs-7 fw-normal ">
        Note: Hello sir I am trying my best to develop the application but sir
        in database showing some error not connecting the Mongo DB. If you
        upload an image and video successfully upload to Cloudinary. Because the
        database does not render the web page. But in the console attach the
        secure_url.
      </span>
    </div>
  );
}

export default Note;
